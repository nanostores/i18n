import { transform } from '../transforms/index.js'

// oxlint-disable-next-line no-unused-vars <temp>
export const messageFormat = transform((locale, translation, params) => {
  // TODO extract message parts
  // TODO format with params
})
